{
  "compilerOptions": {
    "baseUrl": ".",          // 追加: ルートをプロジェクト直下に
    "paths": {
      "@/*": ["src/*"]       // 追加: "@/..." → "src/..." へ解決
    },
    "jsx": "react-jsx",      // 省略されていれば追加すると React import 不要
    /* 既存の設定はそのまま */
  }
}
